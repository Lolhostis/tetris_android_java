<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.iut.dut2.tetris">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TetrisApp">
        <activity
            android:name=".application.views.MainWindow"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".application.views.GrilleWindowActivity">
        </activity>
        <activity android:name=".application.views.LeaderboardWindow">
        </activity>
        <activity android:name=".application.views.OptionsWindow">
        </activity>
        <activity android:name=".application.views.PauseWindow">
        </activity>
        <activity android:name=".application.views.ConfirmationWindow">
        </activity>
    </application>

</manifest>